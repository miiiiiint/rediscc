# Redis C++ Version 

## Prebuild

- OS: Ubuntu 24.10(at least Ubuntu 22.04)

``` bash
sudo apt-get install cmake gdb lldb clang clang-tools clang-format automake autogen autoconf build-essential pkg-config
```

**clang tool must be clang-16**.
